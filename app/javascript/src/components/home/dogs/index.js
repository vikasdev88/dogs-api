import React from "react"
import DogsContainer from "./DogsContainer"

const Dogs = (props) => {
  return (
    <React.Fragment>
      < DogsContainer />
    </React.Fragment>
  )
}


export default Dogs
